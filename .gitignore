# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies (automatically installed via package manager)
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Testing (coverage reports and test artifacts)
/coverage

# Next.js and Build Output (generated files)
/.next/
/out/
/build

# Environment Variables (sensitive credentials)
.env*
.env
.env*.local
!.env.example
# Do not commit any .env files to git, except for the .env.example file
# https://create.t3.gg/en/usage/env-variables#using-environment-variables

# Debug Logs (local debugging information)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# IDE and Editor Files (personal settings)
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.DS_Store        # macOS system files
*.pem            # SSL certificates

# TypeScript (generated type definitions)
*.tsbuildinfo
next-env.d.ts

# Vercel (deployment metadata)
.vercel

# Cache and Temporary Files (local performance optimizations)
.cache
.temp
Thumbs.db        # Windows image cache